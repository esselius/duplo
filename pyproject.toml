[project]
name = "duplo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bleak>=0.22.3",
    "construct>=2.10.70",
    "ipython>=8.31.0",
    "pydantic>=2.10.4",
]
[dependency-groups]
dev = [
    "pytest-watcher>=0.4.3",
    "pytest>=8.3.4",
]
